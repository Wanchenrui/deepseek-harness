/**
 * @deepseek-ai/dsh-power-desktop — static Power Desktop patch layer. Its
 * runtime substance is `cordis.patch.yml`, resolved through `dsh.bundle.patch`;
 * this module intentionally exports no runtime API.
 * @module @deepseek-ai/dsh-power-desktop
 */

export {}
